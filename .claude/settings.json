{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "{ git add -A -- backend frontend runtime .github docs docker-compose.yml AGENTS.md CLAUDE.md .dockerignore .gitignore && (git diff --cached --quiet || git commit -m \"auto: session $(date -u +%Y%m%dT%H%M%SZ)\"); } 2>/dev/null || true",
            "statusMessage": "Auto-committing session changes..."
          }
        ]
      }
    ]
  }
}
